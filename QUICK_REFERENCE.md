# 🚀 Быстрая шпаргалка по Xcode

## ⌨️ Горячие клавиши

### Основные
- **Cmd + R** - Запустить приложение
- **Cmd + .** - Остановить приложение
- **Cmd + B** - Собрать проект
- **Cmd + Shift + K** - Очистить сборку
- **Cmd + /** - Закомментировать/раскомментировать строку

### Навигация
- **Cmd + 1** - Project Navigator
- **Cmd + 2** - Source Control Navigator
- **Cmd + 3** - Search Navigator
- **Cmd + 4** - Issue Navigator (ошибки)
- **Cmd + Shift + O** - Быстрый поиск файла/символа
- **Cmd + Click** - Перейти к определению

### Редактирование
- **Cmd + F** - Поиск в файле
- **Cmd + Shift + F** - Поиск во всем проекте
- **Cmd + /** - Комментарий
- **Cmd + D** - Дублировать строку
- **Cmd + Delete** - Удалить строку
- **Option + Cmd + [** - Переместить строку вверх
- **Option + Cmd + ]** - Переместить строку вниз

### Git
- **Cmd + Option + C** - Commit
- **Cmd + Shift + P** - Push
- **Cmd + Shift + X** - Pull

---

## 📱 Запуск приложения

1. Выберите симулятор в верхней панели
2. Нажмите **▶️ Play** (Cmd + R)
3. Дождитесь запуска

---

## 🔍 Поиск и замена

- **Cmd + F** - Поиск в текущем файле
- **Cmd + Shift + F** - Поиск во всем проекте
- **Cmd + Option + F** - Замена в текущем файле
- **Cmd + Option + Shift + F** - Замена во всем проекте

---

## 🐛 Отладка

- **F6** - Шаг вперед (Step Over)
- **F7** - Шаг внутрь (Step Into)
- **F8** - Шаг наружу (Step Out)
- **Cmd + \** - Поставить/убрать точку останова
- **Cmd + Y** - Включить/выключить все точки останова

---

## 📂 Создание файлов

1. Правый клик на папку → **New File...**
2. Выберите тип:
   - **Swift File** - обычный Swift файл
   - **SwiftUI View** - новый экран
   - **Cocoa Touch Class** - класс (для UIKit)
3. Введите имя
4. Выберите папку
5. **Create**

---

## 🎨 SwiftUI Preview

1. Откройте файл с `View`
2. Нажмите **Resume** в правом верхнем углу
3. Или используйте **Option + Cmd + P**

---

## 🔄 Git операции

### В Xcode:
- **Source Control → Commit...** - Коммит
- **Source Control → Push...** - Отправить на GitHub
- **Source Control → Pull...** - Получить с GitHub

### В Терминале:
```bash
git status                    # Статус изменений
git add .                     # Добавить все файлы
git commit -m "Сообщение"     # Коммит
git push                      # Отправить на GitHub
git pull                      # Получить с GitHub
```

---

## 📝 Основные конструкции Swift

### Переменные
```swift
let constant = "Нельзя изменить"
var variable = "Можно изменить"
```

### Функция
```swift
func name(parameter: String) -> String {
    return "Result"
}
```

### Структура
```swift
struct Product {
    let id: String
    let name: String
}
```

### Класс с ObservableObject
```swift
class ViewModel: ObservableObject {
    @Published var data: [String] = []
}
```

### SwiftUI View
```swift
struct MyView: View {
    var body: some View {
        Text("Hello")
    }
}
```

---

## 🆘 Частые проблемы

| Проблема | Решение |
|----------|---------|
| "No such module" | Проверьте Frameworks в настройках проекта |
| Симулятор не запускается | Скачайте симулятор в Xcode → Settings → Platforms |
| Ошибки компиляции | Cmd + Shift + K (Clean), затем Cmd + B (Build) |
| Git не работает | Проверьте `git config --list` в Терминале |

---

## 📚 Полезные ссылки

- [Swift Documentation](https://swift.org/documentation/)
- [SwiftUI Tutorials](https://developer.apple.com/tutorials/swiftui)
- [Hacking with Swift](https://www.hackingwithswift.com/)

---

## ✅ Быстрый чеклист

- [ ] Проект открыт в Xcode
- [ ] Симулятор выбран
- [ ] Приложение запускается (Cmd + R)
- [ ] Preview работает
- [ ] Git настроен

