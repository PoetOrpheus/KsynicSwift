# 🔧 Исправление ошибок в Xcode

## ❌ Ошибка: "Cannot find SplashScreen in scope" / "Cannot find MainScreen in scope"

Эта ошибка возникает, когда Xcode не видит файлы, хотя они физически существуют в проекте.

### 🔍 Причина

Файлы добавлены в папку проекта, но не добавлены в **Target** проекта. Xcode компилирует только файлы, которые включены в Target.

---

## ✅ Решение

### Способ 1: Проверка Target Membership (рекомендуется)

1. **Откройте файл `SplashScreen.swift`** в Xcode
2. В правой панели (Inspector) найдите раздел **"Target Membership"**
3. Убедитесь, что стоит **галочка** напротив `KsynicSwift` (название вашего проекта)
4. Если галочки нет - поставьте её
5. Повторите для `MainScreen.swift`

### Способ 2: Добавление файлов через Project Navigator

1. В **Project Navigator** (левая панель) найдите файлы `SplashScreen.swift` и `MainScreen.swift`
2. Выберите файл (например, `SplashScreen.swift`)
3. В правой панели (Inspector) проверьте **Target Membership**
4. Если галочки нет - поставьте

### Способ 3: Массовое добавление файлов в Target

1. В **Project Navigator** выберите папку `UI/Screens`
2. Правый клик → **Get Info** (или **Cmd + I**)
3. В открывшемся окне найдите **Target Membership**
4. Поставьте галочку напротив `KsynicSwift`
5. Нажмите **Done**

### Способ 4: Пересоздание ссылок на файлы

Если файлы все еще не видны:

1. **Удалите файлы из проекта** (НЕ удаляйте физически!):
   - Выберите файл в Project Navigator
   - Нажмите **Delete**
   - Выберите **Remove Reference** (НЕ "Move to Trash"!)

2. **Добавьте файлы заново:**
   - Правый клик на папку `UI/Screens` в Project Navigator
   - **Add Files to "KsynicSwift"...**
   - Выберите файлы `SplashScreen.swift` и `MainScreen.swift`
   - ⚠️ **ВАЖНО**: Убедитесь, что стоит галочка **"Add to targets: KsynicSwift"**
   - Нажмите **Add**

---

## 🔍 Проверка других файлов

Проверьте, что все файлы добавлены в Target:

- ✅ `SplashScreen.swift`
- ✅ `MainScreen.swift`
- ✅ `HomeScreen.swift`
- ✅ `ShopCartScreen.swift`
- ✅ `FavoriteScreen.swift`
- ✅ `ProfileScreen.swift`
- ✅ Все файлы в `Models/`
- ✅ Все файлы в `ViewModels/`
- ✅ Все файлы в `Repository/`
- ✅ Все файлы в `Navigation/`
- ✅ Все файлы в `Theme/`
- ✅ Все файлы в `Data/`

---

## 🛠️ Дополнительные проверки

### Проверка 1: Build Phases

1. Выберите проект в **Project Navigator** (самый верхний элемент)
2. Выберите **Target** `KsynicSwift`
3. Перейдите на вкладку **Build Phases**
4. Разверните **Compile Sources**
5. Убедитесь, что там есть все `.swift` файлы

Если файлов нет:
- Нажмите **+** внизу
- Добавьте нужные файлы

### Проверка 2: Clean Build

После исправления:

1. **Product → Clean Build Folder** (или **Cmd + Shift + K**)
2. **Product → Build** (или **Cmd + B**)
3. Проверьте, что ошибок нет

---

## 🐛 Если ошибка все еще есть

### Проверка импортов

Убедитесь, что в файлах есть правильные импорты:

```swift
import SwiftUI  // Для всех View файлов
import Foundation  // Для моделей и других файлов
```

### Проверка структуры файлов

Убедитесь, что структуры объявлены правильно:

**SplashScreen.swift:**
```swift
struct SplashScreen: View {
    // ...
}
```

**MainScreen.swift:**
```swift
struct MainScreen: View {
    // ...
}
```

### Проверка зависимостей

Убедитесь, что все зависимости доступны:

- `BottomNavItem` - должен быть в `Navigation/BottomNavItem.swift`
- `ProductViewModel` - должен быть в `ViewModels/ProductViewModel.swift`
- `Color.brandPurple` - должен быть в `Theme/Colors.swift`

---

## ✅ Чеклист исправления

- [ ] Проверен Target Membership для `SplashScreen.swift`
- [ ] Проверен Target Membership для `MainScreen.swift`
- [ ] Проверены все файлы в папке `UI/Screens`
- [ ] Выполнен Clean Build (Cmd + Shift + K)
- [ ] Выполнен Build (Cmd + B)
- [ ] Ошибки исчезли

---

## 🚀 После исправления

1. Запустите проект (Cmd + R)
2. Проверьте, что приложение работает
3. Если все хорошо - сделайте коммит:
   ```bash
   git add .
   git commit -m "Fix: добавлены файлы в Target"
   git push
   ```

---

**Если проблема не решается - напишите, какие именно ошибки вы видите!**

